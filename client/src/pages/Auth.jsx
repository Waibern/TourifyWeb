import{useState}from'react';import{Link,useNavigate}from'react-router-dom';import{Eye,EyeOff,ArrowRight}from'lucide-react';import toast from'react-hot-toast';import{useAuth}from'../contexts/AuthContext';import{Logo}from'../components/Shared';
function AuthShell({register=false}){const[show,setShow]=useState(false),[f,setF]=useState({name:'',username:'',identity:'',email:'',password:'',confirm:''}),{login,register:signup}=useAuth(),nav=useNavigate();const submit=async e=>{e.preventDefault();if(register&&f.password!==f.confirm)return toast.error('Passwords do not match');try{const u=register?await signup({name:f.name,username:f.username,email:f.email,password:f.password}):await login({identity:f.identity,password:f.password});toast.success(register?'Account created successfully':'Login successful');nav(u.role==='admin'?'/admin':'/')}catch(e){toast.error(e.response?.data?.message||'Please try again')}};const field=(name,label,type='text')=><label>{label}<input type={type} value={f[name]} onChange={e=>setF({...f,[name]:e.target.value})} required/></label>;return <main className="auth"><section className="auth-picture"><Logo/><div><p className="kicker">EASIER BOOKING, ANYWHERE.</p><h1>Indonesia is<br/><em>waiting</em> for you.</h1><p>Book unforgettable experiences, all in one beautiful place.</p></div></section><section className="auth-form"><div><Logo/><p className="kicker">{register?'CREATE YOUR ACCOUNT':'WELCOME BACK'}</p><h1>{register?'Start exploring.':'Sign in to continue exploring.'}</h1><form onSubmit={submit}>{register&&<>{field('name','Full name')}{field('username','Username')}</>}{field(register?'email':'identity',register?'Email':'Username or email',register?'email':'text')}<label>Password<div className="password"><input type={show?'text':'password'} value={f.password} onChange={e=>setF({...f,password:e.target.value})} required minLength="6"/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label>{register&&field('confirm','Confirm password','password')}<button className="button full" type="submit">{register?'Create Account':'Login'} <ArrowRight size={18}/></button></form><p className="switch">{register?'Already have an account?':'Don’t have an account?'} <Link to={register?'/login':'/register'}>{register?'Sign in':'Create Account'}</Link></p></div></section></main>};export const Login=()=> <AuthShell/>;export const Register=()=> <AuthShell register/>
